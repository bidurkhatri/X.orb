/**
 * @deprecated These service files are not currently imported by any UI component.
 * The frontend uses inline wagmi hooks with inline ABIs instead.
 * 
 * TODO: Either refactor UI components to use these service abstractions,
 * or remove these files entirely if the inline approach is preferred.
 * 
 * Dead service files (~83KB total):
 * - bridgeService.ts
 * - defiService.ts
 * - governanceService.ts
 * - identityService.ts
 * - nftService.ts
 * - stakingService.ts
 */

// Placeholder — these modules use named exports, not default.
// Import them directly if integrating: import { xxx } from './xxxService'
export { }
