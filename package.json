{
  "name": "xorb",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @xorb/api dev",
    "build": "pnpm --filter @xorb/api build",
    "test": "pnpm -r test",
    "test:contracts": "pnpm --filter xorb-contracts test",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": "20.x",
    "pnpm": ">=8.0.0"
  }
}
